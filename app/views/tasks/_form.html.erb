<%= form_with(model: task) do |form| %>
  <% if task.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(task.errors.count, "error") %> prohibited this task from being saved:</h2>

      <ul>
        <% task.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :prompt, style: "display: block", class: "form-label" %>
    <%= form.text_field :prompt, class: "form-control" %>
  </div>

  <div>
    <%= form.label :completion, style: "display: block", class: "form-label" %>
    <%= form.text_area :completion, class: "form-control" %>
  </div>

  <div>
    <%= form.submit 'Submit', class: "m-2 p-1 btn btn-primary" %>
  </div>
<% end %>
